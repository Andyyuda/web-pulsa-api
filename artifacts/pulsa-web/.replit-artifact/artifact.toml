kind = "web"
previewPath = "/"
title = "Pulsa Web"
version = "1.0.0"
id = "artifacts/pulsa-web"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20441

[services.development]
run = "pnpm --filter @workspace/pulsa-web run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/pulsa-web", "run", "build" ]
publicDir = "artifacts/pulsa-web/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20441"
BASE_PATH = "/"
